#include "MathFunctions.h"

double mysqrt(const double to_be_square_off) {
	std::cout << "you're using my sqrt function!" << std::endl;
	const double out_value = sqrt(to_be_square_off);
	return out_value;
}
